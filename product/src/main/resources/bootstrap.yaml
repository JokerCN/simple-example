spring:
  application:
    name: product-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        #namespace:
        #group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848